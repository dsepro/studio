
"use client";

// import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
// import { Slider } from "@/components/ui/slider";
// import { Icons } from "@/components/icons";
// import React from "react";

// interface SettingsCardProps {
//   fontScale: number;
//   onFontScaleChange: (scale: number) => void;
// }

// export function SettingsCard({ fontScale, onFontScaleChange }: SettingsCardProps) {
//   const handleSliderChange = (value: number[]) => {
//     onFontScaleChange(value[0]);
//   };

//   return (
//     <Card>
//       <CardHeader>
//         <CardTitle className="text-xl md:text-2xl">Settings</CardTitle>
//       </CardHeader>
//       <CardContent className="space-y-6">
//         {/* Font size functionality moved to header */}
//       </CardContent>
//     </Card>
//   );
// }

// This component is no longer used as its functionality (font size) has been moved to the header.
// It can be removed from the project if no other settings are planned for this card.
// For now, I'm commenting it out. If you want to delete it, you can do so.
export function SettingsCard() {
  return null;
}
